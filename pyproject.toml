[tool.poetry]
name = "party-wall-flask"
version = "0.1.0"
description = ""
authors = ["Hubert Kasprowski <hkasprowski@pgs-soft.com>"]

[tool.poetry.dependencies]
python = "^3.8"
Flask = "^1.1.2"
Flask-RESTful = "^0.3.8"
flask-sqlalchemy = "^2.4.4"

[tool.poetry.dev-dependencies]
black = "^20.8b1"
pylint = "^2.6.0"
mypy = "^0.790"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
